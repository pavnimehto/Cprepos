#include <stdio.h>
int main()
{
    int i, n, sum=0, count=0;
    printf("Enter a number:");
    scanf("%d", &n);
    for (i=1; count<n; i++)
    {
        if(i % 2 == 0)
        {sum = sum + i;
            count++;}
        else
        {continue;
    }
    } 
    printf("%d", sum);


}
