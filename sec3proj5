#include <stdio.h>
int main()
{
    int i=0, count=0, n;
    printf("Enter the value of n:");
    scanf("%d", &n);

    while (count< 10)
    {i++;
        if (i % 2 == 1)
        {printf("%d\n", i);
        count++;}
        else
        {continue;
        }
    }

}
