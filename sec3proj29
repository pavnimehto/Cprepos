#include <stdio.h>
int main()
{
    int n,f=0, i;
    printf("Enter a number:");
    scanf("%d", &n);
    for (i=2; i<n; i++)
    {
        if (n%i==0)
        {f=1;}
        else 
        {continue;}
    }
    if (f==1)
    {printf("%d is not a prime number", n);}
    else
    {printf("%d is a prime number", n);}
}
