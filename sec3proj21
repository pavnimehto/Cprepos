#include <stdio.h>
int main()
{
    int n, i;
    printf("What are the number of digits in your number:");
    scanf("%d", &n);

    char dig[n];
    printf("Enter the number:");
    scanf("%s", dig);

    for (i=0; i<n; i++)
    {
        printf("%c,", dig[i]);
    }
    return 0;

}
