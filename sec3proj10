#include <stdio.h>
int main()
{
    int i, n, fact=1, count;
    printf("Enter a number:");
    scanf("%d", &n);
    for (i=1; count<=n; i++)
    {
        fact= fact* i;
        count++;
    }
    printf("%d", fact);

}
