#include <stdio.h>

int main() {
    double bytes, kb, mb, gb;

    printf("Enter size in bytes: ");
    scanf("%lf", &bytes);

    kb = bytes / 1024;
    mb = kb / 1024;
    gb = mb / 1024;

    printf("%.2lf Bytes = %.2lf KB = %.2lf MB = %.2lf GB\n", bytes, kb, mb, gb);

    return 0;
}
