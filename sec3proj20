#include <stdio.h>
int main()
{
    int sum=0, i, arr[100];
    for (i=1; i<=100; i++)
    {
        if(i%3==0)
        {
            arr[i]= i;
            sum+= arr[i];
        }
        else 
        {continue;}
    }
    printf("%d", sum);
}
