#include<stdio.h>
int main()
{
    int count=0, i=0, n;
    printf("Enter value of n:");
    scanf("%d", &n);

    while (count<n)
    {
        i++;
        if (i % 2 == 0)
        {printf("%d\n", i);
        count++;}
        else 
        {continue;
        }
    }


}
