#include <stdio.h>
#include <string.h>

int main()
{
    int i, dig=0;
    char arr[100];
    printf("Enter your number:");
    scanf("%s", arr);
    for (i=0; i<strlen(arr); i++)
    {
        dig += 1;
    }
    printf("There are %d digits in the given number.", dig);

}
